## 이벤트 처리
- on 메소드 사용

```
    // 클릭 이벤트
    $("button").on("click", function() {
        alert("클릭 이벤트 발생");
    })

    // 마우스 오버 이벤트
    $("p").on("mouseover", function() {
        $(this).css("background-color", "yellow");
    })∂
```